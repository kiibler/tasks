import { LuListTodo } from "react-icons/lu";

const Logo = () => {
    return (
        <div className="flex justify-center items-center h-28 border-b-2 border-gray-300">
            <LuListTodo size={60} />
        </div>
    );
};

export default Logo;
