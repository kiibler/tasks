import { LuListTodo } from "react-icons/lu";

const Logo = () => {
    return (
        <div className="flex h-28 items-center justify-center border-b-2 border-gray-300">
            <LuListTodo size={60} />
        </div>
    );
};

export default Logo;
